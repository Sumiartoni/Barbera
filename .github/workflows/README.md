# Workflows

Tambahkan CI/CD berikut pada tahap berikutnya:

- `ci.yml` untuk build dan test Go + Next.js
- `security.yml` untuk dependency scan, SAST, dan CodeQL
- `deploy.yml` untuk staging/production rollout

